import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.boogie.driverapp',
  appName: 'Boogie Driver',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
